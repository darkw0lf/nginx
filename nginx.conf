user www-data;
worker_processes auto;
worker_cpu_affinity auto;
pid /run/nginx.pid;
error_log /var/log/nginx/error.log;
include /etc/nginx/modules-enabled/*.conf;

events {
        worker_connections 768;
}

http {

        types_hash_max_size 2048;
        server_tokens on; # Recommended practice is to turn this off

        include /etc/nginx/mime.types;
        default_type application/octet-stream;

        ssl_protocols TLSv1.2 TLSv1.3; # Dropping SSLv3 (POODLE), TLS 1.0, 1.1
        ssl_prefer_server_ciphers off; # Don't force server cipher order.

        access_log /var/log/nginx/access.log;

        gzip on;

        include /etc/nginx/conf.d/*.conf;
        include /etc/nginx/sites-enabled/*;

        # Performance
        sendfile on;
        tcp_nopush on; # envoyer les en-têtes en un seul paquet
        tcp_nodelay on; # désactiver l'algorithme de Nagle

        # Keep-alive
        keepalive_timeout 65; # secondes
        keepalive_requests 1000; # requêtes max par connexion keep-alive

        # Tailles / timeouts client
        client_max_body_size 10m;
        client_body_timeout 30;
        client_header_timeout 30;
        send_timeout 30;
        #more_clear_headers Server; # avec headers-more
}
